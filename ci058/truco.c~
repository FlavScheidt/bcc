#include "socket.h"

int main (int argc, char *argv[])
{
        int maquina;
        if (strcmp(argv[1], "1") == 0)
                maquina = 1;
        else
                maquina = 0;
        
        fprintf(stdout, "%d", maquina);
        
        SetRing(maquina);
        
       // if (maquina == 0)
       //         Send();
        //else
          //      Get();
}
